var timerDiv = document.querySelector(".timer"); var time = 60; var timerStart = document.querySelector(".timer__start"); timerStart.addEventListener('click', start); timerDiv.innerHTML = "<span> 01:00 </span>"; function start() { var timerId = setInterval(function () { var minutes = Math.floor(time / 60); var seconds = time % 60; minutes = ("0" + minutes).slice(-2); seconds = ("0" + seconds).slice(-2); timerDiv.innerHTML = "<span>" + minutes + ":" + seconds + "</span>"; if (time == 0) { clearInterval(timerId); } --time; }, 1000); }
